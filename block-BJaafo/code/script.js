function fullName(firstname,secondname){
    return firstname + secondname;
}
let fullname = fullName(venkata,ravindra);

if(fullname !== "venkata ravindra"){
    console.log(error);
}

function totalTex(amount,texrate){
    return amount+(amount*texrate);
}
